# CFT Funnel Relay
Serverless relay: funnel page form POST -> GoHighLevel (contact + tags + note + opportunity + task).
Deployed as a DigitalOcean App Platform functions component. Secrets via app env vars (GHL_KEY, GHL_LOCATION, PIPELINE_ID, STAGE_ID).
Owned by Central Florida Trimlight. Interim until DealerOS PR #267 ships post-freeze.
